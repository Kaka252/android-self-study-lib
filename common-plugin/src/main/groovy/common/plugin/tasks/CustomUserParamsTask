package common.plugin.tasks

import org.gradle.api.DefaultTask
import org.gradle.api.tasks.TaskAction

class CustomUserParamsTask extends DefaultTask {

    @TaskAction
    void printUserParams() {
        println "我的名字叫${project.userParams.userName}, 我的工作是${project.userParams.position}"
    }
}